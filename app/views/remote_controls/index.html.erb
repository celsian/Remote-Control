<% provide(:title, 'Welcome') %>
<% if @remote_controls %>
  <div id="container" style="text-align: center;">
    <span class="btn-group-vertical btn-group-lg">
      <% @remote_controls.each do |remote_control| %>
        <% if remote_control.name == "Gate" %>
            <%= link_to remote_control.name, remote_control.enabled ? remote_control_open_path(remote_control) : "", class: remote_control.enabled ? "btn btn-success" : "btn btn-default disabled", remote: true %>
        <% else %>
          <%= link_to remote_control.name, remote_control.enabled ? remote_control_open_path(remote_control) : "", data: { confirm: "Are you sure you want to open the #{remote_control.name}?" }, class: remote_control.enabled ? "btn btn-success" : "btn btn-default disabled", remote: true %>
        <% end %>
      <% end %>
    </span>
    <span class="btn-group-vertical btn-group-lg">
      <% @remote_controls.each do |remote_control| %>
        <% unless remote_control.name == "Gate" %>
          <%= link_to "Head", remote_control.enabled ? remote_control_head_open_path(remote_control) : "", class: remote_control.enabled ? "btn btn-success" : "btn btn-default disabled", remote: true %>
        <% else %>
          <%= link_to "N/A", "", class: "btn btn-default disabled", remote: true %>
        <% end %>
      <% end %>
    </span>
    <br><br>
    <div class="panel panel-default">
      <div class="panel-body">
        <object data="http://konvett.com:8009/clean.php" width="100%" height="410">
          <embed src="http://konvett.com:8009/clean.php" width="100%" height="410"> </embed>
        </object>
      </div>
    </div>
  </div>
<% end %>
<!-- 
        <%= image_tag("http://celsian.zapto.org:3001", alt: "Refresh to restart stream.", style: "max-width:100%;") %>
        <br>
        <small>Stream will automatically disconnect after 60 seconds to prevent data overages. Refresh to restart.</small> -->